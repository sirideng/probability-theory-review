import type { MachineLearningLesson } from '../types/content'

export const regressionPracticeLessons: MachineLearningLesson[] = [
  {
    id: 'convergence-learning-rate', number: '09', title: '收敛判断与学习率选择', englishTitle: 'Convergence & Learning Rate', videoRange: '视频 22—23',
    summary: '用代价函数曲线检查梯度下降是否正常工作，再比较不同数量级的学习率，在稳定下降与训练速度之间取得平衡。',
    problem: '梯度下降运行很多次后，怎样判断它是否仍在有效下降？如果代价函数值上升、反复波动或下降极慢，又该如何判断学习率需要调大还是调小？',
    intuition: [
      '横轴记录迭代次数，纵轴记录每次更新后的代价函数值 J。算法正常工作时，代价函数值总体应下降并逐渐趋于平缓。',
      '学习率过小通常不会立刻出错，但曲线下降很慢；学习率过大可能跨过低点，使代价函数值震荡甚至发散。',
      '比较相邻两次代价函数值变化是否小于阈值 ε，是一个实用的启发式停止条件，不是已经到达最优解的数学证明。',
      '代价函数值变化很小可能表示接近收敛，也可能只是学习率过小；应结合代价函数曲线、最大迭代次数以及必要时的梯度大小共同判断。ε 没有适用于所有问题的固定值。',
      '选择学习率时可以按约 3 倍或 10 倍的数量级逐步试验，例如 0.001、0.003、0.01、0.03、0.1，而不是只在很窄的范围里盲调。',
    ],
    formulas: [
      { label: '代价函数值变化量', formula: '\\Delta J^{(t)}=\\left|J^{(t)}-J^{(t-1)}\\right|', explanation: '相邻两次代价函数值变化越小，表示当前更新带来的目标函数变化越有限。', symbols: [{ symbol: 't', meaning: '当前迭代编号' }, { symbol: 'J^{(t)}', meaning: '第 t 次更新对应的代价函数值' }, { symbol: '\\Delta J^{(t)}', meaning: '相邻两次代价函数值的绝对变化量' }] },
      { label: '启发式停止条件', formula: '\\left|J^{(t)}-J^{(t-1)}\\right|\\leq\\varepsilon', explanation: '满足阈值只能作为停止信号；它不证明已经到达最优解，应与曲线、最大迭代次数和必要时的梯度大小结合。', symbols: [{ symbol: '\\varepsilon', meaning: '预先设定的正停止阈值，无通用固定值' }, { symbol: 'J', meaning: '使用当前参数计算的训练集代价' }] },
      { label: '学习率控制更新幅度', formula: '\\mathbf w_{\\text{new}}=\\mathbf w-\\alpha\\nabla_{\\mathbf w}J,\\qquad b_{\\text{new}}=b-\\alpha\\frac{\\partial J}{\\partial b}', explanation: '梯度指向局部增长最快方向；减去梯度沿负梯度更新，α>0 负责缩放更新幅度。', symbols: [{ symbol: '\\alpha', meaning: '学习率超参数，满足 α>0' }, { symbol: '\\nabla_{\\mathbf w}J', meaning: 'J 对全部权重的梯度向量' }] },
    ],
    algorithm: [
      { title: '记录初始代价函数值', detail: '在第一次参数更新前计算 J，作为比较基线。' },
      { title: '每轮保存代价函数值', detail: '用更新后的参数重新计算 J，并绘制代价函数曲线。' },
      { title: '先排除发散', detail: '若代价函数值上升、出现 NaN 或持续增大，优先减小学习率并检查实现。' },
      { title: '比较下降速度', detail: '若曲线稳定但过慢，尝试更大的候选学习率。' },
      { title: '设置停止条件', detail: '用最大迭代数配合 |ΔJ|≤ε；必要时还检查梯度大小，避免把小学习率误判为收敛。' },
    ],
    example: {
      title: '比较两次更新后的代价函数值变化',
      setup: '考虑一元目标函数 J(w)=(w-3)²，初始 w₀=0，学习率 α=0.2。',
      steps: [
        { text: '初始导数与代价函数值。', formula: 'J(0)=9,\\qquad J\\prime(0)=2(0-3)=-6' },
        { text: '第一次更新。', formula: 'w_1=0-0.2(-6)=1.2,\\qquad J(w_1)=3.24' },
        { text: '第二次更新。', formula: 'w_2=1.2-0.2\\times2(1.2-3)=1.92,\\qquad J(w_2)=1.1664' },
        { text: '代价函数值继续下降，但尚未平缓。', formula: '\\Delta J^{(2)}=|1.1664-3.24|=2.0736' },
      ],
      result: '当前变化量仍较大，不能据此判断接近收敛；继续迭代时代价函数值会逐渐接近 0。',
    },
    python: {
      code: [
        'import numpy as np',
        '',
        'x = np.array([-1.0, 0.0, 1.0])',
        'y = np.array([-1.0, 1.0, 3.0])',
        'w, b = -2.0, -1.0',
        'alpha, epsilon = 0.15, 1e-6',
        'previous_cost = np.inf',
        '',
        'for iteration in range(1, 1001):',
        '    error = w * x + b - y',
        '    cost = np.mean(error ** 2) / 2',
        '    if abs(previous_cost - cost) <= epsilon:',
        '        break',
        '    dw = np.mean(error * x)',
        '    db = np.mean(error)',
        '    w -= alpha * dw',
        '    b -= alpha * db',
        '    previous_cost = cost',
        '',
        'print(iteration)',
        'print(round(w, 6), round(b, 6))',
        'print(f"{cost:.8f}")',
      ].join('\n'),
      output: '68\n1.996562 0.999963\n0.00000394',
      notes: ['每轮先计算当前代价函数值，再用同一组旧参数计算梯度。', '最大迭代数和 ε 同时存在；ε 只是启发式停止条件，不证明已经到达最优解。', '改变 α 后应重新从同一起点运行，才能公平比较。'],
    },
    interaction: 'convergence-learning-rate',
    misconceptions: [
      { wrong: '代价函数值下降一次或满足阈值就证明已经收敛。', why: '一次下降只说明这一步有效；变化很小也可能来自过小学习率。', correct: '把阈值作为启发式信号，并结合曲线、最大迭代次数和必要时的梯度大小判断。' },
      { wrong: '学习率越小越安全，所以应该尽可能小。', why: '过小会让训练耗费大量迭代。', correct: '选择能保持稳定下降的较大学习率，而不是机械追求最小。' },
      { wrong: '代价函数值上升一定是公式写错。', why: '公式正确时，过大学习率同样可能使更新越过低点。', correct: '同时检查实现与学习率，并用更小 α 重试。' },
    ],
    recap: ['代价函数曲线是重要的训练诊断工具。', '代价函数值通常应总体下降并逐渐趋于平缓。', '|ΔJ|≤ε 是启发式停止条件，不是最优性证明，且 ε 没有通用固定值。', '过小 α 可能很慢，过大 α 可能震荡或发散。', '结合曲线、最大迭代次数与必要时的梯度大小判断收敛。'],
  },
  {
    id: 'feature-engineering-polynomial-regression', number: '10', title: '特征工程与多项式回归', englishTitle: 'Feature Engineering & Polynomial Regression', videoRange: '视频 24—25',
    summary: '利用领域知识组合或变换原始输入，可以把模型看不到的关系显式提供出来；多项式回归就是把 x²、x³ 等作为新特征交给线性回归。',
    problem: '房价与面积的关系可能明显弯曲，地块价值也可能由长和宽共同决定。只把原始特征直接放进直线模型，无法表达这些结构。',
    intuition: [
      '特征工程是从已有特征设计新特征，例如用临街宽度×纵深得到地块面积，或用速度÷限速得到相对速度。',
      '多项式回归把 x、x²、x³ 等看成不同输入特征，再对这些新特征使用多元线性回归。',
      '多项式回归关于原始输入 x 可以是非线性的，但关于参数 w₁、w₂、w₃ 仍然是线性的，因此仍可使用线性回归的代价函数与训练方法。',
      '幂次会迅速放大尺度差异：若 x 在 1 到 1000 之间，x² 可到 10⁶，x³ 可到 10⁹。使用梯度下降训练时，高次特征通常应分别缩放，以减少优化困难。',
    ],
    formulas: [
      { label: '组合特征', formula: 'x_3=x_1x_2', explanation: '把两个原始量的共同作用编码成一个新特征，例如长×宽得到面积。', symbols: [{ symbol: 'x_1,x_2', meaning: '两个原始特征' }, { symbol: 'x_3', meaning: '根据领域知识构造的新特征' }] },
      { label: '三次多项式回归', formula: 'f_{\\mathbf w,b}(x)=w_1x+w_2x^2+w_3x^3+b', explanation: '输入从一个原始 x 扩展为 [x,x²,x³]，模型仍是这些特征的加权和。', symbols: [{ symbol: 'x', meaning: '原始特征' }, { symbol: 'x^2,x^3', meaning: '由原始特征生成的多项式特征' }, { symbol: 'w_1,w_2,w_3', meaning: '各项对应权重' }] },
      { label: '平方根也是特征变换', formula: 'f_{\\mathbf w,b}(x)=w_1x+w_2\\sqrt{x}+b', explanation: '特征工程不限于整数幂；应依据数据形状和问题含义选择变换。', symbols: [{ symbol: '\\sqrt{x}', meaning: '当 x≥0 时可构造的非线性特征' }] },
    ],
    algorithm: [
      { title: '理解原始变量', detail: '先明确每个输入的单位、范围和现实含义。' },
      { title: '提出有意义的组合', detail: '尝试乘积、比值、幂或根号等能表达领域关系的变换。' },
      { title: '生成新特征矩阵', detail: '保持原始样本一一对应，只增加或替换特征列。' },
      { title: '逐列缩放', detail: '使用梯度下降时，对 x²、x³ 等量级差异大的列分别计算缩放参数。' },
      { title: '继续使用线性回归', detail: '在扩展后的特征上计算预测、代价和梯度。' },
    ],
    example: {
      title: '把弯曲关系写成线性参数模型',
      setup: '假设房价模型为 ŷ=1+2x+0.5x²，房屋面积特征 x=3。',
      steps: [
        { text: '构造新特征。', formula: '\\mathbf x_{\\text{poly}}=[x,x^2]=[3,9]' },
        { text: '写出权重和偏置。', formula: '\\mathbf w=[2,0.5],\\qquad b=1' },
        { text: '用点积计算预测。', formula: '\\hat y=2\\times3+0.5\\times9+1=11.5' },
      ],
      result: '预测曲线关于原始 x 是二次的；但计算仍是新特征 [x,x²] 与权重的点积加偏置。',
    },
    python: {
      code: [
        'import numpy as np',
        '',
        'x = np.arange(5, dtype=float)',
        'y = 1 + 2 * x + 0.5 * x ** 2',
        '',
        'X_poly = np.column_stack([np.ones_like(x), x, x ** 2])',
        'coefficients, *_ = np.linalg.lstsq(X_poly, y, rcond=None)',
        '',
        'print(X_poly[3])',
        'print(coefficients.round(6))',
        'print(X_poly @ coefficients)',
      ].join('\n'),
      output: '[1. 3. 9.]\n[1.  2.  0.5]\n[ 1.   3.5  7.  11.5 17. ]',
      notes: ['第一列全为 1，对应偏置 b。', 'x 和 x² 是两列不同特征。', '这里的 np.linalg.lstsq 最小二乘数值求解仅用于验证多项式特征能够拟合目标关系，不属于本节训练主线；当前课程主要使用梯度下降训练参数。'],
    },
    interaction: 'polynomial-regression',
    misconceptions: [
      { wrong: '多项式回归不再属于线性回归。', why: '“线性”描述的是参数进入模型的方式，而不是曲线对原始 x 是否笔直。', correct: '它是对多项式特征做线性回归。' },
      { wrong: '使用梯度下降时，x、x²、x³ 的尺度差异无需考虑。', why: '幂次越高，数值范围可能相差越大，使梯度下降变慢或不稳定。', correct: '使用梯度下降训练时，通常分别缩放生成后的特征列。' },
      { wrong: '特征工程就是随意增加很多公式。', why: '无意义变换只会增加复杂度，未必提供有效信息。', correct: '优先使用有现实含义、能表达已知关系的组合。' },
    ],
    recap: ['特征工程利用领域知识设计新输入。', '乘积、比值、幂和根号都可以成为候选变换。', '多项式回归关于原始输入可以非线性，但关于参数仍然线性。', '使用梯度下降时，高次特征通常应分别缩放。', 'np.linalg.lstsq 示例只验证模型表达，不属于当前训练主线。'],
    probabilityLink: { label: '概率论关联：随机变量是函数', to: '/knowledge/random-variable' },
  },
]
