import type { MachineLearningLesson } from '../types/content'
import { regressionPracticeLessons } from './machine-learning-course-regression-practice.ts'
import { classificationLessons } from './machine-learning-course-classification.ts'

export const machineLearningLessons: MachineLearningLesson[] = [
  {
    id: 'machine-learning-basics', number: '01', title: '认识机器学习', englishTitle: 'Understanding Machine Learning', videoRange: '视频 1—2',
    summary: '机器学习不是把规则逐条写进程序，而是让程序从示例数据中学习一组可用于新输入的参数。',
    problem: '房屋面积与价格之间存在规律，但很难为每一种面积手写价格规则。我们希望利用已有成交记录，得到一个能对新房屋作出预测的模型。',
    intuition: [
      '传统程序通常由人写出规则，再把输入交给规则得到输出；机器学习则把数据和期望结果交给训练算法，让算法寻找合适的模型参数。',
      '训练和使用模型是两个阶段：训练阶段从历史数据学习参数，预测阶段固定这些参数，对新输入计算结果。',
      '当规律会随数据变化、规则难以枚举或需要从大量样本中总结模式时，机器学习更有价值；若规则明确且稳定，普通程序通常更简单可靠。',
    ],
    formulas: [{
      label: '训练与预测的基本关系', formula: '\\mathcal D\\xrightarrow{\\text{训练算法}}\\theta\\xrightarrow{\\text{组成模型}}f_\\theta(x)=\\hat y',
      explanation: '训练数据决定参数，参数组成模型；新输入经过模型后得到预测，而不是重新训练一次。',
      symbols: [{ symbol: '\\mathcal D', meaning: '训练数据集' }, { symbol: '\\theta', meaning: '模型学到的参数' }, { symbol: 'x', meaning: '一个新输入' }, { symbol: '\\hat y', meaning: '模型给出的预测' }],
    }],
    algorithm: [
      { title: '准备训练数据', detail: '收集房屋面积以及对应成交价格，让模型看到输入与结果的配对。' },
      { title: '选择模型形式', detail: '先用直线表达面积与价格的近似关系，明确模型可以调整哪些参数。' },
      { title: '学习参数', detail: '训练算法比较预测与真实价格，逐步找到误差较小的参数。' },
      { title: '固定模型并预测', detail: '输入一套新房屋的面积，使用已学到的参数输出预测价格。' },
    ],
    example: { title: '从三条成交记录得到预测任务', setup: '已有面积 50、80、110 m² 的房屋，成交价分别为 100、160、220 万元。', steps: [{ text: '把面积看作输入 x，把成交价看作目标 y。' }, { text: '观察到面积每增加 30 m²，价格大约增加 60 万元。' }, { text: '用这一规律预测 95 m² 房屋的价格。', formula: '\\hat y=2\\times95=190' }], result: '预测约为 190 万元；这是根据样本规律得到的估计，不是保证成交价一定为 190 万元。' },
    python: { code: `import numpy as np\n\nx = np.array([50., 80., 110.])\ny = np.array([100., 160., 220.])\nw, b = 2.0, 0.0\nnew_house = 95.0\nprediction = w * new_house + b\nprint(prediction)`, output: '190.0', notes: ['x 和 y 保存训练样本。', 'w、b 是已经学到的模型参数。', 'prediction 是对新房屋的预测，不会改变模型参数。'] },
    interaction: 'none',
    misconceptions: [
      { wrong: '模型就是训练数据本身。', why: '数据是学习材料，模型是从数据中提取出的参数化规律。', correct: '数据用于训练，训练后的模型用于新输入。' },
      { wrong: '所有问题都应该使用机器学习。', why: '规则明确的问题使用普通程序更直接，也更容易验证。', correct: '只有在需要从数据中学习规律时再使用机器学习。' },
    ],
    recap: ['机器学习从数据中学习参数。', '训练与预测是两个不同阶段。', '模型输出是估计，不是确定事实。', '本路线始终用房价预测串联后续概念。'],
  },
  {
    id: 'supervised-unsupervised', number: '02', title: '监督学习与无监督学习', englishTitle: 'Supervised & Unsupervised Learning', videoRange: '视频 3—5',
    summary: '是否提供目标标签，决定模型是在学习输入到答案的映射，还是只寻找数据自身的结构。',
    problem: '同样是一张房屋表，如果表中有成交价格，我们可以学习预测价格；如果没有价格，就只能从房屋特征中寻找相似群体。',
    intuition: ['训练样本是一条可供模型学习的数据记录；特征描述样本，标签表示希望模型预测的正确答案。', '监督学习使用带标签样本。输出连续数值称为回归，输出离散类别称为分类；此处只建立区别，不展开分类算法。', '无监督学习没有目标标签。算法不会得到“正确答案”，而是尝试发现相似性、分组或其他结构；聚类是最直观的例子。', 'Jupyter Notebook 只是组织文字、代码和结果的学习环境，不改变算法本身。'],
    formulas: [{ label: '两种数据形式', formula: '\\text{监督学习：}(x^{(i)},y^{(i)})\\qquad\\text{无监督学习：}x^{(i)}', explanation: '上标 (i) 表示第 i 个样本；监督学习额外拥有目标 y。', symbols: [{ symbol: 'x^{(i)}', meaning: '第 i 个样本的特征' }, { symbol: 'y^{(i)}', meaning: '第 i 个样本的标签或目标' }, { symbol: 'i', meaning: '样本编号' }] }],
    algorithm: [{ title: '先看是否有标签', detail: '有目标列时可以学习输入到目标的映射；没有目标列时不能直接训练预测器。' }, { title: '判断目标类型', detail: '连续房价对应回归；有限类别对应分类。' }, { title: '明确输出', detail: '监督学习输出预测，无监督学习输出分组或结构描述。' }],
    example: { title: '同一批房屋的两种任务', setup: '三套房屋记录了面积与卧室数，其中一份表还记录了成交价格。', steps: [{ text: '有价格表：用面积、卧室数预测价格，属于监督学习中的回归。' }, { text: '无价格表：按面积和卧室数寻找相似房屋群，属于无监督学习中的聚类。' }, { text: '两种任务输入相似，但输出问题完全不同。' }], result: '标签不是普通特征；它定义了监督学习希望预测的答案。' },
    python: { code: `import numpy as np\n\nfeatures = np.array([[60, 2], [90, 3], [130, 4]])\nprices = np.array([120, 180, 260])\n\nprint(features.shape)\nprint(prices.shape)`, output: '(3, 2)\n(3,)', notes: ['features 有 3 个样本、每个样本 2 个特征。', 'prices 有 3 个标签，与样本一一对应。', '删除 prices 后，数据本身不会自动告诉算法要预测什么。'] },
    interaction: 'supervised-toggle',
    misconceptions: [{ wrong: '没有标签就无法进行任何学习。', why: '无监督学习仍能从特征中寻找结构。', correct: '没有标签意味着任务不是学习已知答案，而不是没有任何信息。' }, { wrong: '分类就是把数据分成几组。', why: '监督分类依赖已知类别标签，聚类没有标签。', correct: '先看训练数据中是否提供类别答案。' }],
    recap: ['特征描述输入，标签定义要预测的目标。', '回归预测连续值，分类预测类别。', '无监督学习从无标签数据中寻找结构。', 'Notebook 是环境，不是学习算法。'],
  },
  {
    id: 'linear-regression-model', number: '03', title: '线性回归模型', englishTitle: 'Linear Regression Model', videoRange: '视频 6',
    summary: '单变量线性回归用一条可调直线近似输入与连续目标之间的关系。',
    problem: '房屋面积增加时价格往往上升，但训练点不会完全落在同一条直线上。我们需要一条简单直线概括总体趋势并预测新面积。',
    intuition: ['w 决定直线每向右移动一个单位上升多少，b 决定 x=0 时直线从哪里开始。', '训练点的 y 是真实成交价，直线上的 f(x) 是预测值；二者之间的竖直距离是预测误差。', '调整 w、b 会同时改变所有样本的预测，因此必须用一个汇总指标比较不同直线。'],
    formulas: [{ label: '单变量线性回归', formula: 'f_{w,b}(x)=wx+b=\\hat y', explanation: '把输入乘以斜率并加上截距，得到连续数值预测。', symbols: [{ symbol: 'x', meaning: '输入特征，例如房屋面积' }, { symbol: 'w', meaning: '权重或斜率' }, { symbol: 'b', meaning: '偏置或截距' }, { symbol: 'f_{w,b}(x)', meaning: '模型对 x 的预测' }, { symbol: 'y', meaning: '真实目标值' }, { symbol: '\\hat y', meaning: '预测值' }] }],
    algorithm: [{ title: '接收输入', detail: '读入一套房屋的面积 x。' }, { title: '乘以权重', detail: 'w 决定面积变化对价格预测的影响幅度。' }, { title: '加上偏置', detail: 'b 整体上移或下移预测直线。' }, { title: '输出预测', detail: '结果是估计价格，再与真实价格比较误差。' }],
    example: { title: '手工预测一套房屋', setup: '将面积按“百平方米”计，模型参数 w=100、b=50，新房屋面积 x=1.2。', steps: [{ text: '把参数和输入代入模型。', formula: 'f_{w,b}(1.2)=100\\times1.2+50' }, { text: '先计算斜率贡献，再加偏置。', formula: '120+50=170' }], result: '模型预测价格为 170 万元。若真实价格为 180 万元，误差为 -10 万元。' },
    python: { code: `import numpy as np\n\nx = np.array([0.5, 1.0, 1.5])\ny = np.array([95., 150., 210.])\nw, b = 100.0, 50.0\npredictions = w * x + b\nerrors = predictions - y\nprint(predictions)\nprint(errors)`, output: '[100. 150. 200.]\n[  5.   0. -10.]', notes: ['NumPy 会把同一个 w、b 应用于全部 x。', 'errors 的正负表示预测高于或低于真实值。', '下一节会把这些误差汇总为成本。'] },
    interaction: 'linear-regression',
    misconceptions: [{ wrong: '拟合直线必须穿过所有训练点。', why: '真实数据含有噪声，一条直线通常无法覆盖全部点。', correct: '线性回归寻找总体误差较小的直线。' }, { wrong: 'b 是房屋面积为 0 时真实存在的价格。', why: '截距可能只承担数学上的整体平移，超出数据范围时未必有现实意义。', correct: '在训练范围内解释和使用模型更可靠。' }],
    recap: ['w 控制斜率，b 控制整体平移。', '预测值与真实值必须区分。', '一组参数对应一条直线。', '比较模型需要汇总全部样本误差。'],
  },
  {
    id: 'cost-function', number: '04', title: '成本函数', englishTitle: 'Cost Function', videoRange: '视频 7—9',
    summary: '成本函数把所有样本的预测误差压缩成一个可比较的数字，使“哪组参数更好”成为可计算的问题。',
    problem: '不同的直线会在不同样本上产生不同误差。只看一个点无法判断整条直线，因此需要同时考虑全部训练样本。',
    intuition: ['预测误差是预测值减真实值；平方让正负误差不会抵消，并更重视较大的偏差。', '对全部样本求和后除以 m 得到平均水平，额外的 1/2 让后续求导中的 2 抵消，不改变最优参数。', '成本越小表示这组参数在当前训练数据上的总体拟合越好；成本为 0 表示全部训练样本预测完全正确。'],
    formulas: [{ label: '平方误差成本', formula: 'J(w,b)=\\frac{1}{2m}\\sum_{i=1}^{m}\\left(f_{w,b}(x^{(i)})-y^{(i)}\\right)^2', explanation: '同一组 w、b 先为每个样本预测，再汇总平方误差。', symbols: [{ symbol: 'm', meaning: '训练样本总数' }, { symbol: 'i', meaning: '样本编号' }, { symbol: 'x^{(i)}', meaning: '第 i 个输入' }, { symbol: 'y^{(i)}', meaning: '第 i 个真实目标' }, { symbol: 'f_{w,b}(x^{(i)})-y^{(i)}', meaning: '第 i 个预测误差' }, { symbol: 'J(w,b)', meaning: '当前参数的总体成本' }] }],
    algorithm: [{ title: '逐个预测', detail: '使用同一组参数计算每个训练样本的预测值。' }, { title: '计算误差', detail: '预测减真实值，保留方向信息。' }, { title: '误差平方', detail: '避免正负抵消，并放大严重误差。' }, { title: '求和并平均', detail: '得到可在不同参数之间比较的一个标量。' }],
    example: { title: '三个样本的成本', setup: '训练数据为 (1,2)、(2,4)、(3,6)，当前模型 f(x)=1.5x。', steps: [{ text: '三个预测分别为 1.5、3、4.5，误差为 -0.5、-1、-1.5。' }, { text: '平方误差之和。', formula: '(-0.5)^2+(-1)^2+(-1.5)^2=3.5' }, { text: '除以 2m，其中 m=3。', formula: 'J=\\frac{3.5}{2\\times3}\\approx0.5833' }], result: '当前成本约为 0.5833；若改用 w=2、b=0，则成本为 0。' },
    python: { code: `import numpy as np\n\nx = np.array([1., 2., 3.])\ny = np.array([2., 4., 6.])\nw, b = 1.5, 0.0\npredictions = w * x + b\nerrors = predictions - y\ncost = np.sum(errors ** 2) / (2 * len(x))\nprint(cost)`, output: '0.5833333333333334', notes: ['predictions 对应 f(x)。', 'errors ** 2 对应公式中的平方项。', 'len(x) 就是样本数 m。'] },
    interaction: 'cost-function',
    misconceptions: [{ wrong: '成本就是某一个点的误差。', why: '成本汇总了全部训练样本。', correct: '单点有误差，整组数据对应成本。' }, { wrong: '成本小就代表未来预测一定好。', why: '当前成本只衡量训练数据上的拟合。', correct: '本阶段先用成本训练模型，但不要把训练结果解释成绝对保证。' }],
    recap: ['成本把多条误差汇总成一个标量。', '平方避免正负误差抵消。', '1/2 方便求导，不改变最优解。', '参数空间中的每个点都对应一个成本。'],
  },
  {
    id: 'gradient-descent', number: '05', title: '梯度下降', englishTitle: 'Gradient Descent', videoRange: '视频 10—12',
    summary: '梯度下降根据成本函数在当前位置的斜率，反复把参数向成本降低的方向移动。',
    problem: '成本函数告诉我们当前参数有多差，却没有直接给出更好的参数。梯度提供局部方向，让模型可以一步步降低成本。',
    intuition: ['梯度为正表示参数继续增大会让成本上升，因此减去正梯度会向左移动；梯度为负时，减去负数会向右移动。', '梯度只描述当前位置附近的方向，一次更新通常不会直接到达最小值，所以需要重复计算。', 'w 和 b 的梯度必须基于同一组旧参数计算后同步更新，否则第二个更新会混入已经改变的参数。', '一般函数可能有多个局部低点；但本路线中的线性回归平方误差关于参数是凸函数，采用合适学习率时，梯度下降的目标是全局最小值。'],
    formulas: [{ label: '同步参数更新', formula: 'w:=w-\\alpha\\frac{\\partial J}{\\partial w},\\qquad b:=b-\\alpha\\frac{\\partial J}{\\partial b}', explanation: '学习率决定步长，梯度决定方向。', symbols: [{ symbol: '\\alpha', meaning: '学习率，每次更新的步长系数' }, { symbol: '\\partial J/\\partial w', meaning: '成本对 w 的局部斜率' }, { symbol: '\\partial J/\\partial b', meaning: '成本对 b 的局部斜率' }, { symbol: ':=', meaning: '用右侧结果更新左侧参数' }] }, { label: '线性回归梯度', formula: '\\frac{\\partial J}{\\partial w}=\\frac1m\\sum_{i=1}^m(f_{w,b}(x^{(i)})-y^{(i)})x^{(i)},\\quad\\frac{\\partial J}{\\partial b}=\\frac1m\\sum_{i=1}^m(f_{w,b}(x^{(i)})-y^{(i)})', explanation: '每个样本的误差都参与梯度；w 的梯度还要乘对应输入。', symbols: [{ symbol: 'm', meaning: '样本数' }, { symbol: 'f-y', meaning: '当前预测误差' }, { symbol: 'x^{(i)}', meaning: '第 i 个输入对斜率更新的影响' }] }],
    algorithm: [{ title: '选择初始参数', detail: '从 w、b 的一个起点开始。' }, { title: '计算当前成本', detail: '记录当前位置的模型误差。' }, { title: '计算两个梯度', detail: '使用当前参数计算 w、b 的局部斜率。' }, { title: '同步更新', detail: '用同一轮梯度同时产生新 w、新 b。' }, { title: '重复', detail: '在新位置重新计算成本和梯度。' }],
    example: { title: '手算一次梯度更新', setup: '当前 w=0，b=0，已算得 ∂J/∂w=-4、∂J/∂b=-2，学习率 α=0.1。', steps: [{ text: '更新 w。', formula: 'w_{new}=0-0.1\\times(-4)=0.4' }, { text: '更新 b。', formula: 'b_{new}=0-0.1\\times(-2)=0.2' }, { text: '两个梯度为负，因此两个参数都向正方向移动。' }], result: '新参数为 w=0.4、b=0.2。应在新参数处重新计算成本，而不是重复使用旧梯度。' },
    python: { code: `import numpy as np\n\nx = np.array([2.0])\ny = np.array([2.0])\n\ndef gradient_step(x, y, w, b, alpha):\n    predictions = w * x + b\n    errors = predictions - y\n    dw = np.mean(errors * x)\n    db = np.mean(errors)\n    new_w = w - alpha * dw\n    new_b = b - alpha * db\n    return new_w, new_b\n\nw, b = gradient_step(x, y, 0.0, 0.0, 0.1)\nprint(w, b)`, output: '0.4 0.2', notes: ['这个单样本恰好产生 dw=-4、db=-2，与手算例题一致。', 'dw、db 都使用旧的 w、b 计算。', '函数只执行一步；多次训练需要循环调用。'] },
    interaction: 'gradient-descent',
    misconceptions: [{ wrong: '梯度直接指向最终最低点。', why: '梯度只给出当前位置上升最快的局部方向。', correct: '每走一步都要重新计算梯度；线性回归平方误差的凸性保证这里没有需要绕开的次优局部低点。' }, { wrong: '先更新 w，再用新 w 计算 b 的梯度。', why: '这破坏了同步更新。', correct: '先保存两个梯度，再同时生成新参数。' }],
    recap: ['减去梯度是为了向成本下降方向移动。', '一次更新与完整训练不同。', '学习率控制步长。', 'w 与 b 必须同步更新。', '线性回归平方误差是凸函数，合适步长下目标是全局最小值。'],
  },
  {
    id: 'learning-rate-training', number: '06', title: '学习率与线性回归训练', englishTitle: 'Learning Rate & Training', videoRange: '视频 13—15',
    summary: '学习率决定每次沿梯度移动多远；把模型、成本和梯度更新循环起来，才构成完整的线性回归训练。',
    problem: '方向正确并不代表步长合适。步子太小会训练缓慢，步子过大可能越过低点并来回震荡，甚至让成本越来越大。',
    intuition: ['较小学习率通常能稳定下降，但需要更多迭代；合适学习率能在稳定与速度之间取得平衡。', '过大学习率可能反复跨过低点，每一步离低点更远，表现为成本震荡或发散。', '这里先建立训练循环与基本直觉；模块 09 会进一步使用成本曲线检查收敛，并系统比较候选学习率。'],
    formulas: [{ label: '训练循环', formula: '(w_t,b_t)\\xrightarrow{\\text{预测与成本}}J_t\\xrightarrow{\\text{梯度}}(w_{t+1},b_{t+1})', explanation: '每轮都使用新参数重新预测、计算成本和梯度。', symbols: [{ symbol: 't', meaning: '当前迭代次数' }, { symbol: 'J_t', meaning: '第 t 次迭代的成本' }, { symbol: 'w_t,b_t', meaning: '第 t 次迭代使用的参数' }] }],
    algorithm: [{ title: '初始化 w、b', detail: '选择训练起点。' }, { title: '前向预测', detail: '用当前直线预测全部样本。' }, { title: '计算成本与梯度', detail: '衡量模型并得到更新方向。' }, { title: '按 α 更新', detail: '学习率缩放梯度，形成新参数。' }, { title: '继续迭代', detail: '观察直线和成本随迭代变化。' }],
    example: { title: '同一梯度下的三种步长', setup: '当前位置 w=0，梯度为 -4。', steps: [{ text: 'α=0.01：', formula: 'w_{new}=0-0.01(-4)=0.04' }, { text: 'α=0.1：', formula: 'w_{new}=0-0.1(-4)=0.4' }, { text: 'α=1：', formula: 'w_{new}=0-1(-4)=4' }], result: '三次移动方向相同，但距离差异很大；大步长可能直接跨过低成本区域。' },
    python: { code: `import numpy as np\n\nx = np.array([-1.0, 0.0, 1.0])\ny = np.array([-1.0, 1.0, 3.0])\nw, b = 0.0, 0.0\nalpha = 0.1\n\nfor iteration in range(50):\n    predictions = w * x + b\n    errors = predictions - y\n    dw = np.mean(errors * x)\n    db = np.mean(errors)\n    w = w - alpha * dw\n    b = b - alpha * db\n\nprint(round(w, 6), round(b, 6))`, output: '1.936486 0.994846', notes: ['这组三点恰好来自直线 y=2x+1，因此参数逐步接近 w=2、b=1。', 'for 循环把一次更新变成完整训练过程。', 'alpha 是正的步长系数，但并不存在必须小于 1 的通用规定；可用范围由当前问题的尺度决定。', '本节不引入后续课程的系统收敛诊断方法。'] },
    interaction: 'learning-rate',
    misconceptions: [{ wrong: '学习率越大，训练一定越快。', why: '过大步长可能跨过低点并发散。', correct: '速度必须建立在稳定下降的基础上。' }, { wrong: '学习率会改变数据本身。', why: '它只控制参数更新步长。', correct: '数据固定，w、b 随训练变化。' }],
    recap: ['小学习率稳定但可能缓慢。', '过大学习率可能震荡或发散。', '线性回归训练是预测、计算成本、求梯度和更新的循环。', '完成训练循环后，可以继续用成本曲线诊断收敛。'],
  },
  {
    id: 'multiple-features-vectorization', number: '07', title: '多特征与向量化', englishTitle: 'Multiple Features & Vectorization', videoRange: '视频 16—19',
    summary: '当每套房屋由面积、卧室数和楼龄共同描述时，向量和点积可以用一个统一表达式完成多项乘法与加法。',
    problem: '只用面积可能忽略卧室数和楼龄。为每个特征手写一条独立公式会迅速变得冗长，因此需要向量化表达。',
    intuition: ['一个样本的多个特征组成向量 x，每个特征都有一个对应权重 w_j；权重数量必须与特征数量一致。', '点积把每对对应元素相乘后求和，相当于一次完成 w₁x₁+w₂x₂+⋯。', '单个样本 x.shape=(n,)；整个数据集通常是 m 行 n 列。np.dot(w,x) 对一个样本输出标量。', '多特征梯度下降为每个 w_j 计算各自梯度，再与 b 同步更新。'],
    formulas: [{ label: '多元线性回归', formula: 'f_{\\mathbf w,b}(\\mathbf x)=\\mathbf w^{\\mathsf T}\\mathbf x+b=\\sum_{j=1}^{n}w_jx_j+b', explanation: '向量内积是对应特征贡献的总和。', symbols: [{ symbol: '\\mathbf x', meaning: '一个样本的 n 个特征' }, { symbol: '\\mathbf w', meaning: 'n 个特征权重' }, { symbol: 'x_j', meaning: '第 j 个特征值' }, { symbol: 'w_j', meaning: '第 j 个特征的权重' }, { symbol: 'n', meaning: '特征数量' }, { symbol: 'b', meaning: '单独更新的偏置' }] }, { label: '每个权重的梯度', formula: '\\frac{\\partial J}{\\partial w_j}=\\frac1m\\sum_{i=1}^m(f_{\\mathbf w,b}(\\mathbf x^{(i)})-y^{(i)})x_j^{(i)}', explanation: '不同特征拥有不同梯度，但所有权重应同步更新。', symbols: [{ symbol: 'm', meaning: '训练样本数量' }, { symbol: 'n', meaning: '每个样本的特征数量' }, { symbol: 'j', meaning: '特征编号' }, { symbol: 'i', meaning: '样本编号' }, { symbol: 'x_j^{(i)}', meaning: '第 i 个样本的第 j 个特征' }] }],
    algorithm: [{ title: '组织一个样本', detail: '把面积、卧室数、楼龄按固定顺序组成 x。' }, { title: '准备对应权重', detail: 'w 与 x 使用同一顺序和相同长度。' }, { title: '计算点积', detail: '对应元素相乘后求和，再加 b。' }, { title: '批量训练', detail: '对 m 个样本汇总误差，为每个 w_j 计算梯度并同步更新。' }],
    example: { title: '三特征房价预测', setup: 'x=[120,3,10] 分别表示面积、卧室数、楼龄；w=[1.2,8,-0.5]，b=20。', steps: [{ text: '逐项贡献。', formula: '1.2\\times120=144,\\quad8\\times3=24,\\quad(-0.5)\\times10=-5' }, { text: '求和并加偏置。', formula: '\\hat y=144+24-5+20=183' }], result: '预测价格为 183 万元；楼龄权重为负表示在其他特征固定时，楼龄增加会降低预测。' },
    python: { code: `import numpy as np\n\nx = np.array([120., 3., 10.])\nw = np.array([1.2, 8.0, -0.5])\nb = 20.0\n\nloop_prediction = 0.0\nfor j in range(len(x)):\n    loop_prediction += w[j] * x[j]\nloop_prediction += b\n\nvectorized_prediction = np.dot(w, x) + b\nprint(x.shape, w.shape)\nprint(loop_prediction, vectorized_prediction)`, output: '(3,) (3,)\n183.0 183.0', notes: ['循环和 np.dot 得到完全相同的数学结果。', 'x.shape 与 w.shape 都是 (3,)，点积输出一个标量。', '向量化让公式更接近数学表达，也便于处理更多特征。'] },
    interaction: 'vectorization',
    misconceptions: [{ wrong: '向量化只是把循环换个写法，没有数学意义。', why: '点积明确表达多个特征贡献的总和。', correct: '向量化同时改善表达和计算组织。' }, { wrong: '整个数据集和单个样本形状相同。', why: '数据集包含 m 个样本。', correct: '单个样本常为 (n,)，数据集常为 (m,n)。' }],
    recap: ['每个特征对应一个权重。', '点积等于对应乘积之和。', 'np.dot(w,x) 对单个样本输出标量。', '多重线性回归同步更新所有权重和偏置。'],
  },
  {
    id: 'feature-scaling', number: '08', title: '特征缩放', englishTitle: 'Feature Scaling', videoRange: '视频 20—21',
    summary: '把不同数量级的特征调整到相近尺度，可以让成本等高线不再过度狭长，使梯度下降路径更直接。',
    problem: '房屋面积约为 300—3000，卧室数约为 1—5。若直接训练，同一个步长对两个权重产生的实际影响差异很大，参数容易在狭长成本谷中来回震荡。',
    intuition: ['特征缩放主要改变训练效率，不会让原始数据更准确，也不会改变样本之间的相对次序。', '按范围缩放直接除以一个代表性上界，简单但不保证居中。均值归一化先减均值，再除以极差。', 'Z-score 先减均值，再除以标准差，使每个特征通常具有接近 0 的均值和接近 1 的标准差。', '目标值是否缩放属于另一个问题，本节不展开；也不引入数据泄漏、Pipeline 等后续工程主题。'],
    formulas: [
      { label: '按范围缩放', formula: 'x_1^{\\text{scaled}}=\\frac{x_1}{3000}', explanation: '使用已知的代表性范围把面积压缩到较小数量级，但不一定以 0 为中心。', symbols: [{ symbol: 'x_1', meaning: '原始面积特征' }, { symbol: '3000', meaning: '选用的面积尺度上界' }] },
      { label: '均值归一化', formula: 'x_j^{\\text{scaled}}=\\frac{x_j-\\mu_j}{x_{j,\\max}-x_{j,\\min}}', explanation: '先居中，再用极差缩放。', symbols: [{ symbol: 'x_j', meaning: '第 j 个特征的原始值' }, { symbol: '\\mu_j', meaning: '第 j 个特征的均值' }, { symbol: 'x_{j,\\max},x_{j,\\min}', meaning: '该特征的最大值与最小值' }] },
      { label: 'Z-score 标准化', formula: 'x_j^{\\text{scaled}}=\\frac{x_j-\\mu_j}{\\sigma_j}', explanation: '当 σ_j>0 时，标准化后该特征的均值为 0、按同一总体标准差口径计算的标准差为 1；有限精度显示时可能出现近似值。', symbols: [{ symbol: 'x_j', meaning: '原始特征值' }, { symbol: '\\mu_j', meaning: '该特征均值' }, { symbol: '\\sigma_j', meaning: '该特征标准差，必须大于 0' }] },
    ],
    algorithm: [{ title: '按列计算尺度', detail: '每个特征分别计算均值、极差或标准差。' }, { title: '逐列变换', detail: '同一特征的所有样本使用相同公式。' }, { title: '用缩放数据训练', detail: '在相近尺度上计算预测、成本和梯度。' }, { title: '比较路径', detail: '相同学习率下，缩放后的梯度下降通常更直接。' }],
    example: { title: '手算一个样本的两种缩放', setup: '面积数据为 [300,1650,3000]，待缩放样本面积为 1650；均值为 1650，极差为 2700，标准差约为 1102.27。', steps: [{ text: '按范围缩放。', formula: '1650/3000=0.55' }, { text: '均值归一化。', formula: '(1650-1650)/(3000-300)=0' }, { text: 'Z-score 标准化。', formula: '(1650-1650)/1102.27=0' }], result: '中间样本经两种居中方法都变为 0；其他样本仍保持原来的大小顺序。' },
    python: { code: `import numpy as np\n\nX = np.array([[300., 1.], [1650., 3.], [3000., 5.]])\nmean = X.mean(axis=0)\nstd = X.std(axis=0)\nz_score = (X - mean) / std\nprint(mean)\nprint(std.round(3))\nprint(z_score.round(3))`, output: '[1650.    3.]\n[1102.27     1.633]\n[[-1.225 -1.225]\n [ 0.     0.   ]\n [ 1.225  1.225]]', notes: ['axis=0 表示分别对每个特征列计算。', '同一列使用自己的均值和标准差。', '缩放改变数值尺度，但保持样本在该特征上的相对顺序。'] },
    interaction: 'feature-scaling',
    misconceptions: [{ wrong: '缩放会让数据更准确。', why: '它只改变表示尺度，不增加信息。', correct: '主要作用是改善梯度下降的训练效率。' }, { wrong: '所有特征都除以同一个数。', why: '不同特征具有不同尺度。', correct: '每一列使用自己的均值、极差或标准差。' }, { wrong: '标准差为 0 的常数特征也能直接做 Z-score。', why: '分母为 0，公式没有定义。', correct: '常数特征不提供区分样本的信息，应单独处理，不能直接相除。' }, { wrong: '缩放会改变样本之间原有关系。', why: '这里的正比例缩放和标准化保持同一特征内的相对次序。', correct: '尺度改变，同一列样本的大小顺序仍保留。' }],
    recap: ['数量级差异会形成狭长成本等高线。', '缩放通常让梯度下降路径更直接。', '均值归一化使用均值和极差。', 'Z-score 使用均值和标准差。', '缩放改善训练效率，不等于提高数据准确性。'],
    probabilityLink: { label: '概率论关联：均值、方差与标准差', to: '/knowledge/variance' },
  },
  ...regressionPracticeLessons,
  ...classificationLessons,
]

export const machineLearningScope = {
  current: '正则化逻辑回归',
  videos: '视频 1—36',
  note: '学习路线参考吴恩达 2022 版 Machine Learning Specialization；本站内容为个人复习笔记与原创交互讲解。',
}

export function getMachineLearningLesson(id: string | undefined) {
  return machineLearningLessons.find((lesson) => lesson.id === id)
}
