import type { MachineLearningLesson } from '../types/content'
import { regressionPracticeLessons } from './machine-learning-course-regression-practice.ts'
import { classificationLessons } from './machine-learning-course-classification.ts'

export const machineLearningLessons: MachineLearningLesson[] = [
  {
    id: 'machine-learning-basics', number: '01', title: '认识机器学习', englishTitle: 'Understanding Machine Learning', videoRange: '视频 1—2',
    summary: '机器学习让系统从数据中学习可用于预测、判断或发现结构的模式，而不需要为每一种输入手写完整规则。',
    problem: '房屋面积与价格之间存在规律，但很难为每一种面积手写价格规则。我们希望利用已有成交记录，得到一个能对新房屋作出预测的模型。',
    intuition: [
      '传统程序通常由人写出规则，再把输入交给规则得到输出；机器学习则利用数据学习可复用的模式。监督学习通常学习从输入到目标的映射，无监督学习尝试发现数据自身的结构。',
      '并不是所有机器学习方法都只能概括成“学习一组参数”。参数化模型会把模式表示为参数；线性回归就是其中一种具体形式。',
      '训练和使用参数化模型是两个阶段：训练算法根据训练集优化参数；参数确定模型后，预测阶段把未见输入送入模型得到输出。',
      '当规律会随数据变化、规则难以枚举或需要从大量样本中总结模式时，机器学习更有价值；若规则明确且稳定，普通程序通常更简单可靠。',
    ],
    formulas: [{
      label: '训练与预测的基本关系', formula: '\\mathcal D\\xrightarrow{\\text{训练算法}}\\theta\\xrightarrow{\\text{组成模型}}f_\\theta(x)=\\hat y',
      explanation: '这是参数化监督模型的基本关系：训练算法根据训练集优化参数，参数确定模型；未见输入经过模型得到预测，而不是重新训练一次。',
      symbols: [{ symbol: '\\mathcal D', meaning: '训练集' }, { symbol: '\\theta', meaning: '训练算法学到的参数' }, { symbol: 'x', meaning: '一个未见输入' }, { symbol: '\\hat y', meaning: '模型给出的预测值' }],
    }],
    algorithm: [
      { title: '准备训练数据', detail: '收集房屋面积以及对应成交价格，让模型看到输入与结果的配对。' },
      { title: '选择模型形式', detail: '先用直线表达面积与价格的近似关系，明确模型可以调整哪些参数。' },
      { title: '学习参数', detail: '训练算法比较预测与真实价格，逐步找到误差较小的参数。' },
      { title: '固定模型并预测', detail: '输入一套新房屋的面积，使用已学到的参数输出预测价格。' },
    ],
    example: { title: '从三条成交记录得到预测任务', setup: '已有面积 50、80、110 m² 的房屋，成交价分别为 100、160、220 万元。', steps: [{ text: '把面积看作输入 x，把成交价看作目标 y。' }, { text: '观察到面积每增加 30 m²，价格大约增加 60 万元。' }, { text: '用这一规律预测 95 m² 房屋的价格。', formula: '\\hat y=2\\times95=190' }], result: '预测约为 190 万元；这是根据样本规律得到的估计，不是保证成交价一定为 190 万元。' },
    python: { code: `import numpy as np\n\nx = np.array([50., 80., 110.])\ny = np.array([100., 160., 220.])\nw, b = 2.0, 0.0\nnew_house = 95.0\nprediction = w * new_house + b\nprint(prediction)`, output: '190.0', notes: ['x 和 y 保存训练样本。', 'w、b 是已经学到的模型参数。', 'prediction 是对新房屋的预测，不会改变模型参数。'] },
    interaction: 'none',
    misconceptions: [
      { wrong: '模型就是训练数据本身。', why: '数据是学习材料，模型是从数据中提取出的参数化规律。', correct: '数据用于训练，训练后的模型用于新输入。' },
      { wrong: '所有问题都应该使用机器学习。', why: '规则明确的问题使用普通程序更直接，也更容易验证。', correct: '当任务需要从数据中学习模式时，机器学习才可能具有优势。' },
    ],
    recap: ['机器学习从数据中学习可复用模式；参数化模型只是其中一类。', '训练算法、模型与预测是不同概念。', '模型输出是估计，不是确定事实。', '本路线使用线性回归这一参数化模型串联后续概念。'],
  },
  {
    id: 'supervised-unsupervised', number: '02', title: '监督学习与无监督学习', englishTitle: 'Supervised & Unsupervised Learning', videoRange: '视频 3—5',
    summary: '监督学习通常学习特征到监督目标的映射；没有目标标签时，无监督学习研究数据自身的结构。',
    problem: '同样是一张房屋表，如果表中有成交价格，可以学习预测连续目标值；如果没有目标标签，则可以研究房屋特征中的聚类、降维或分布结构。',
    intuition: ['训练样本是一条供算法学习的数据记录；特征描述输入。监督目标是概括性称呼：回归任务使用连续目标值，分类任务使用类别标签。', '监督学习使用带监督目标的样本。输出连续数值称为回归，输出离散类别称为分类；此处只建立区别，不展开分类算法。', '无监督学习没有目标标签，可以研究数据自身的结构，例如聚类、降维或分布特征；本节以聚类作为直观示例。', 'Jupyter Notebook 只是组织文字、代码和结果的学习环境，不改变算法本身。'],
    formulas: [{ label: '两种数据形式', formula: '\\text{监督学习：}(\\mathbf x^{(i)},y^{(i)})\\qquad\\text{无监督学习：}\\mathbf x^{(i)}', explanation: '上标 (i) 表示第 i 个样本；监督学习额外拥有目标 y，具体可为回归目标值或分类标签。', symbols: [{ symbol: '\\mathbf x^{(i)}', meaning: '第 i 个样本的特征向量' }, { symbol: 'y^{(i)}', meaning: '第 i 个样本的监督目标' }, { symbol: 'i', meaning: '样本编号' }] }],
    algorithm: [{ title: '先看是否有监督目标', detail: '有目标列时可以学习输入到目标的映射；没有目标列时研究数据自身的结构。' }, { title: '判断目标类型', detail: '连续房价是回归目标值；有限类别是分类标签。' }, { title: '明确输出', detail: '监督学习产生目标预测；无监督学习产生聚类、低维表示或其他结构描述。' }],
    example: { title: '同一批房屋的两种任务', setup: '三套房屋记录了面积与卧室数，其中一份表还记录了成交价格。', steps: [{ text: '有价格表：用面积、卧室数预测连续目标值，属于监督学习中的回归。' }, { text: '无价格表：按面积和卧室数寻找相似房屋群，属于无监督学习中的聚类示例。' }, { text: '两种任务输入相似，但输出问题不同。' }], result: '房价目标值不是普通输入特征；它定义了监督回归希望预测的量。' },
    python: { code: `import numpy as np\n\nfeatures = np.array([[60, 2], [90, 3], [130, 4]])\nprices = np.array([120, 180, 260])\n\nprint(features.shape)\nprint(prices.shape)`, output: '(3, 2)\n(3,)', notes: ['features 有 3 个样本、每个样本 2 个特征。', 'prices 有 3 个连续目标值，与样本一一对应。', '删除 prices 后，数据本身不会自动指定要预测的目标，但仍可研究特征结构。'] },
    interaction: 'supervised-toggle',
    misconceptions: [{ wrong: '没有标签就无法进行任何学习。', why: '无监督学习仍能从特征中寻找结构。', correct: '没有标签意味着任务不是学习已知答案，而不是没有任何信息。' }, { wrong: '分类就是把数据分成几组。', why: '监督分类依赖已知类别标签，聚类没有标签。', correct: '先看训练数据中是否提供类别答案。' }],
    recap: ['特征描述输入；回归使用目标值，分类使用类别标签。', '监督学习通常学习输入到监督目标的映射。', '无监督学习研究无目标标签数据中的结构，聚类只是其中一种形式。', 'Jupyter Notebook 是环境，不是学习算法。'],
  },
  {
    id: 'linear-regression-model', number: '03', title: '线性回归模型', englishTitle: 'Linear Regression Model', videoRange: '视频 6',
    summary: '单变量线性回归用一条可调直线近似输入与连续目标之间的关系。',
    problem: '房屋面积增加时价格往往上升，但训练点不会完全落在同一条直线上。我们需要一条简单直线概括总体趋势，并对训练集中未见过的面积给出预测。',
    intuition: ['w 决定直线每向右移动一个单位上升多少，b 决定 x=0 时直线从哪里开始。', '训练点的 y 是真实成交价，直线上的 f(x) 是预测值；二者之间的竖直距离是预测误差。', '调整 w、b 会同时改变所有样本的预测，因此必须用一个汇总指标比较不同直线。'],
    formulas: [{ label: '单变量线性回归', formula: 'f_{w,b}(x)=wx+b=\\hat y', explanation: '把输入乘以权重并加上偏置，得到实数预测值；在单变量情形下，w 也可解释为斜率。', symbols: [{ symbol: 'x', meaning: '输入特征，例如房屋面积' }, { symbol: 'w', meaning: '权重；单变量情形下也是斜率' }, { symbol: 'b', meaning: '偏置或截距' }, { symbol: 'f_{w,b}(x)', meaning: '模型对 x 的实数预测值' }, { symbol: 'y', meaning: '真实目标值' }, { symbol: '\\hat y', meaning: '预测值' }] }],
    algorithm: [{ title: '接收输入', detail: '读入一套房屋的面积 x。' }, { title: '乘以权重', detail: 'w 决定面积变化对价格预测的影响幅度。' }, { title: '加上偏置', detail: 'b 整体上移或下移预测直线。' }, { title: '输出预测', detail: '结果是估计价格，再与真实价格比较误差。' }],
    example: { title: '手工预测一套房屋', setup: '将面积按“百平方米”计，模型参数 w=100、b=50，新房屋面积 x=1.2。', steps: [{ text: '把参数和输入代入模型。', formula: 'f_{w,b}(1.2)=100\\times1.2+50' }, { text: '先计算斜率贡献，再加偏置。', formula: '120+50=170' }], result: '模型预测价格为 170 万元。若真实价格为 180 万元，误差为 -10 万元。' },
    python: { code: `import numpy as np\n\nx = np.array([0.5, 1.0, 1.5])\ny = np.array([95., 150., 210.])\nw, b = 100.0, 50.0\npredictions = w * x + b\nerrors = predictions - y\nprint(predictions)\nprint(errors)`, output: '[100. 150. 200.]\n[  5.   0. -10.]', notes: ['NumPy 会把同一个 w、b 应用于全部 x。', 'predictions 对应实数预测值，y 对应真实目标值。', 'errors 的正负表示预测高于或低于真实值；下一节会把单样本损失汇总为训练集代价。'] },
    interaction: 'linear-regression',
    misconceptions: [{ wrong: '拟合直线必须穿过所有训练点。', why: '真实数据含有噪声，一条直线通常无法覆盖全部点。', correct: '线性回归通过选择 w 和 b，最小化训练集上的平方误差代价函数。' }, { wrong: 'b 是房屋面积为 0 时真实存在的价格。', why: '截距可能只承担数学上的整体平移，超出数据范围时未必有现实意义。', correct: '在训练范围内解释和使用模型更可靠。' }],
    recap: ['w 控制斜率，b 控制整体平移。', '预测值与真实值必须区分。', '一组参数对应一条直线。', '比较模型需要汇总全部样本误差。'],
  },
  {
    id: 'cost-function', number: '04', title: '代价函数', englishTitle: 'Cost Function', videoRange: '视频 7—9',
    summary: '代价函数汇总整个训练集上的样本损失，使“哪组参数更好”成为可计算的问题。',
    problem: '不同的直线会在不同样本上产生不同误差。只看一个点无法判断整条直线，因此需要同时考虑全部训练样本。',
    intuition: ['预测误差是预测值减真实目标值；平方让正负误差不会抵消，并更重视较大的偏差。', '单个样本可用平方误差损失衡量；对全部样本的平方误差求和，再除以 2m，得到训练集的平方误差代价函数。', '除以 m 得到样本平均，额外的 1/2 用于简化求导；这个常数不会改变最优参数的位置。', '代价函数值越小表示这组参数在当前训练集上的总体拟合越好；代价函数值为 0 表示全部训练样本的预测误差为 0，但不保证对未见数据同样准确。'],
    formulas: [{ label: '单样本平方误差损失', formula: 'L(\\hat y,y)=\\frac12(\\hat y-y)^2', explanation: 'L 衡量一个样本的实数预测值与真实目标值之间的差异。', symbols: [{ symbol: '\\hat y', meaning: '单个样本的预测值' }, { symbol: 'y', meaning: '单个样本的真实目标值' }, { symbol: 'L', meaning: '单样本损失' }] }, { label: '平方误差代价函数', formula: 'J(w,b)=\\frac{1}{2m}\\sum_{i=1}^{m}\\left(f_{w,b}(x^{(i)})-y^{(i)}\\right)^2', explanation: '平方误差之和除以 2m，也可以理解为均方误差的一半。', symbols: [{ symbol: 'm', meaning: '训练样本数量' }, { symbol: 'i', meaning: '样本编号' }, { symbol: 'x^{(i)}', meaning: '第 i 个输入' }, { symbol: 'y^{(i)}', meaning: '第 i 个真实目标值' }, { symbol: 'f_{w,b}(x^{(i)})-y^{(i)}', meaning: '第 i 个预测误差' }, { symbol: 'J(w,b)', meaning: '当前参数对应的训练集代价函数值' }] }],
    algorithm: [{ title: '逐个预测', detail: '使用同一组参数计算每个训练样本的预测值。' }, { title: '计算误差', detail: '预测值减真实目标值，保留方向信息。' }, { title: '误差平方', detail: '避免正负抵消，并提高较大误差的影响。' }, { title: '汇总训练集', detail: '对全部样本的平方误差求和，再除以 2m。' }],
    example: { title: '三个样本的代价函数值', setup: '训练集为 (1,2)、(2,4)、(3,6)，当前模型 f(x)=1.5x。', steps: [{ text: '三个预测分别为 1.5、3、4.5，误差为 -0.5、-1、-1.5。' }, { text: '平方误差之和。', formula: '(-0.5)^2+(-1)^2+(-1.5)^2=3.5' }, { text: '除以 2m，其中 m=3。', formula: 'J=\\frac{3.5}{2\\times3}\\approx0.5833' }], result: '当前代价函数值约为 0.5833；若改用 w=2、b=0，则训练集代价函数值为 0。' },
    python: { code: `import numpy as np\n\nx = np.array([1., 2., 3.])\ny = np.array([2., 4., 6.])\nw, b = 1.5, 0.0\npredictions = w * x + b\nerrors = predictions - y\ncost = np.sum(errors ** 2) / (2 * len(x))\nprint(cost)`, output: '0.5833333333333334', notes: ['predictions 对应 f(x)。', 'errors ** 2 对应公式中的平方项。', 'len(x) 就是样本数 m。'] },
    interaction: 'cost-function',
    misconceptions: [{ wrong: '代价函数就是某一个样本的误差。', why: '单样本差异由损失函数衡量，代价函数汇总整个训练集。', correct: '区分单样本损失 L 与训练集代价 J。' }, { wrong: '训练集代价函数值小就代表未见数据预测一定好。', why: '当前代价函数只衡量训练集上的拟合。', correct: '训练集拟合是训练依据，但不能单独保证泛化表现。' }],
    recap: ['单样本损失 L 与训练集代价 J 需要区分。', '平方避免正负误差抵消。', 'J 是平方误差之和除以 2m，即均方误差的一半。', '额外的 1/2 简化求导且不改变最优参数位置。'],
  },
  {
    id: 'gradient-descent', number: '05', title: '梯度下降', englishTitle: 'Gradient Descent', videoRange: '视频 10—12',
    summary: '梯度给出代价函数局部增长最快的方向；梯度下降反复减去梯度，沿负梯度方向更新参数。',
    problem: '代价函数衡量当前参数的训练集拟合，却没有直接给出下一组参数。梯度描述局部增长方向，负梯度提供局部下降最快的方向。',
    intuition: ['一维导数为正表示参数继续增大会使代价函数值上升，因此减去正导数会向左移动；导数为负时，减去负数会向右移动。多参数情形使用梯度向量。', '梯度只描述当前位置附近增长最快的方向；梯度下降沿负梯度更新，一次更新通常不会直接到达最小值，所以需要重复计算。', 'w 和 b 的梯度应基于同一组旧参数计算后同步更新，否则第二个更新会混入已经改变的参数。', '对于平方误差线性回归，J(w,b) 关于参数是凸二次函数，因此不存在代价函数值更高的次优局部极小点；若设计矩阵不满秩，可能存在多个具有相同最小代价函数值的全局最优解。'],
    formulas: [{ label: '同步参数更新', formula: 'w_{\\text{new}}=w-\\alpha\\frac{\\partial J}{\\partial w},\\qquad b_{\\text{new}}=b-\\alpha\\frac{\\partial J}{\\partial b}', explanation: '学习率缩放更新幅度；减去梯度使参数沿负梯度方向移动。', symbols: [{ symbol: '\\alpha', meaning: '学习率；控制更新幅度且满足 α>0' }, { symbol: '\\partial J/\\partial w', meaning: 'J 对 w 的一维导数' }, { symbol: '\\partial J/\\partial b', meaning: 'J 对 b 的一维导数' }, { symbol: 'w_{\\text{new}},b_{\\text{new}}', meaning: '同步得到的新参数' }] }, { label: '线性回归梯度', formula: '\\frac{\\partial J}{\\partial w}=\\frac1m\\sum_{i=1}^m(f_{w,b}(x^{(i)})-y^{(i)})x^{(i)},\\quad\\frac{\\partial J}{\\partial b}=\\frac1m\\sum_{i=1}^m(f_{w,b}(x^{(i)})-y^{(i)})', explanation: '每个样本的误差都参与梯度；w 的导数还要乘对应输入。', symbols: [{ symbol: 'm', meaning: '训练样本数量' }, { symbol: 'f_{w,b}(x^{(i)})-y^{(i)}', meaning: '第 i 个预测误差' }, { symbol: 'x^{(i)}', meaning: '第 i 个输入对 w 导数的贡献' }] }],
    algorithm: [{ title: '选择初始参数', detail: '从 w、b 的一个起点开始。' }, { title: '计算当前代价函数值', detail: '记录当前位置的训练集代价函数值。' }, { title: '计算两个导数', detail: '使用当前参数计算 J 对 w、b 的局部导数。' }, { title: '同步更新', detail: '用同一轮导数同时得到 w_new、b_new。' }, { title: '重复', detail: '在新位置重新计算代价函数值和梯度。' }],
    example: { title: '手算一次梯度更新', setup: '当前 w=0，b=0，已算得 ∂J/∂w=-4、∂J/∂b=-2，学习率 α=0.1。', steps: [{ text: '更新 w。', formula: 'w_{\\text{new}}=0-0.1\\times(-4)=0.4' }, { text: '更新 b。', formula: 'b_{\\text{new}}=0-0.1\\times(-2)=0.2' }, { text: '两个导数为负，因此两个参数都向正方向移动。' }], result: '新参数为 w=0.4、b=0.2。应在新参数处重新计算代价函数值，而不是重复使用旧梯度。' },
    python: { code: `import numpy as np\n\nx = np.array([2.0])\ny = np.array([2.0])\n\ndef gradient_step(x, y, w, b, alpha):\n    predictions = w * x + b\n    errors = predictions - y\n    dw = np.mean(errors * x)\n    db = np.mean(errors)\n    new_w = w - alpha * dw\n    new_b = b - alpha * db\n    return new_w, new_b\n\nw, b = gradient_step(x, y, 0.0, 0.0, 0.1)\nprint(w, b)`, output: '0.4 0.2', notes: ['这个单样本恰好产生 dw=-4、db=-2，与手算例题一致。', 'dw、db 都使用旧的 w、b 计算。', '函数只执行一步；多次训练需要循环调用。'] },
    interaction: 'gradient-descent',
    misconceptions: [{ wrong: '梯度就是下降方向，并直接指向最终最低点。', why: '梯度给出当前位置增长最快的局部方向；负梯度才是局部下降最快方向。', correct: '每走一步都重新计算梯度；平方误差线性回归的凸性排除了代价函数值更高的次优局部极小点。' }, { wrong: '先更新 w，再用新 w 计算 b 的梯度。', why: '这破坏了同步更新。', correct: '先保存两个导数，再同时得到 w_new 与 b_new。' }],
    recap: ['梯度指向局部增长最快方向，梯度下降沿负梯度更新。', '一次更新与完整训练不同。', '学习率控制更新幅度。', 'w 与 b 应基于同一组旧参数同步更新。', '凸性保证没有代价函数值更高的次优局部极小点，但不满秩时全局最优参数可能不唯一。'],
  },
  {
    id: 'learning-rate-training', number: '06', title: '学习率与线性回归训练', englishTitle: 'Learning Rate & Training', videoRange: '视频 13—15',
    summary: '学习率 α>0 是控制每次参数更新幅度的超参数；把模型预测、代价计算和梯度更新循环起来，才构成完整训练过程。',
    problem: '负梯度方向正确并不代表更新幅度合适。学习率过小可能收敛缓慢；学习率过大可能越过低点并来回震荡，甚至使代价函数值发散。',
    intuition: ['较小学习率通常能稳定下降，但可能需要更多迭代；合适学习率能在稳定与速度之间取得平衡。', '过大学习率可能反复跨过低点，表现为代价函数曲线震荡或发散。', '学习率不存在必须小于 1 的通用规定；合适范围取决于数据尺度、特征尺度和目标函数。', '这里先建立训练循环与基本直觉；模块 09 会进一步使用代价函数曲线检查收敛，并系统比较候选学习率。'],
    formulas: [{ label: '训练循环', formula: '(w_t,b_t)\\xrightarrow{\\text{预测与代价}}J^{(t)}\\xrightarrow{\\text{梯度}}(w_{t+1},b_{t+1})', explanation: '每轮都使用当前参数预测、计算训练集代价与梯度，再同步更新参数。', symbols: [{ symbol: 't', meaning: '当前迭代次数' }, { symbol: 'J^{(t)}', meaning: '第 t 次迭代对应的代价函数值' }, { symbol: 'w_t,b_t', meaning: '第 t 次迭代使用的参数' }] }],
    algorithm: [{ title: '初始化 w、b', detail: '选择训练起点。' }, { title: '前向预测', detail: '用当前模型预测全部训练样本。' }, { title: '计算代价与梯度', detail: '计算训练集代价函数值以及参数梯度。' }, { title: '按 α 更新', detail: '学习率缩放梯度，得到新参数。' }, { title: '继续迭代', detail: '观察预测和代价函数曲线随迭代变化。' }],
    example: { title: '同一导数下的三种更新幅度', setup: '当前位置 w=0，导数为 -4。', steps: [{ text: 'α=0.01：', formula: 'w_{\\text{new}}=0-0.01(-4)=0.04' }, { text: 'α=0.1：', formula: 'w_{\\text{new}}=0-0.1(-4)=0.4' }, { text: 'α=1：', formula: 'w_{\\text{new}}=0-1(-4)=4' }], result: '三次移动方向相同，但距离差异很大；大更新幅度可能跨过低代价区域。' },
    python: { code: `import numpy as np\n\nx = np.array([-1.0, 0.0, 1.0])\ny = np.array([-1.0, 1.0, 3.0])\nw, b = 0.0, 0.0\nalpha = 0.1\n\nfor iteration in range(50):\n    predictions = w * x + b\n    errors = predictions - y\n    dw = np.mean(errors * x)\n    db = np.mean(errors)\n    w = w - alpha * dw\n    b = b - alpha * db\n\nprint(round(w, 6), round(b, 6))`, output: '1.936486 0.994846', notes: ['这组三点恰好来自直线 y=2x+1，因此参数逐步接近 w=2、b=1。', 'for 循环把一次更新变成完整训练过程。', 'alpha 是正的步长系数，但并不存在必须小于 1 的通用规定；可用范围由当前问题的尺度决定。', '本节不引入后续课程的系统收敛诊断方法。'] },
    interaction: 'learning-rate',
    misconceptions: [{ wrong: '学习率越大，训练一定越快。', why: '过大步长可能跨过低点并发散。', correct: '速度必须建立在稳定下降的基础上。' }, { wrong: '学习率会改变数据本身。', why: '它只控制参数更新步长。', correct: '数据固定，w、b 随训练变化。' }],
    recap: ['学习率 α>0 是控制更新幅度的超参数。', '过小 α 可能收敛缓慢，过大 α 可能震荡或发散。', '线性回归训练是预测、计算代价、求梯度和更新参数的循环。', '学习率没有适用于所有问题的固定上界，应结合代价函数曲线诊断。'],
  },
  {
    id: 'multiple-features-vectorization', number: '07', title: '多元线性回归与向量化', englishTitle: 'Multiple Linear Regression & Vectorization', videoRange: '视频 16—19',
    summary: '多元线性回归用向量和矩阵统一表达多个特征的贡献，并利用向量化完成单样本或整批训练集的计算。',
    problem: '只用面积可能忽略卧室数和楼龄。为每个特征手写一条独立公式会迅速变得冗长，因此需要向量化表达。',
    intuition: ['第 i 个样本写成特征向量 \\mathbf x^{(i)}\\in\\mathbb R^n，权重向量 \\mathbf w\\in\\mathbb R^n；每个特征 x_j^{(i)} 对应一个权重 w_j。', '点积把对应特征贡献相乘后求和；f_{\\mathbf w,b}(\\mathbf x) 是一个实数预测值，而不是向量。', '训练集特征矩阵 X\\in\\mathbb R^{m\\times n}，目标向量 \\mathbf y\\in\\mathbb R^m。np.dot(w,x) 计算单个样本的点积并输出标量，X @ w 计算 m 个样本的批量预测并输出长度为 m 的向量。', '向量化把对应特征贡献的求和写成统一的线性代数表达，并可利用底层数值库进行批量计算。'],
    formulas: [{ label: '多元线性回归', formula: 'f_{\\mathbf w,b}(\\mathbf x)=\\mathbf w^{\\mathsf T}\\mathbf x+b=\\sum_{j=1}^{n}w_jx_j+b=\\hat y', explanation: '向量内积汇总对应特征贡献并输出一个实数预测值。', symbols: [{ symbol: '\\mathbf x\\in\\mathbb R^n', meaning: '一个样本的 n 维特征向量' }, { symbol: '\\mathbf w\\in\\mathbb R^n', meaning: 'n 维权重向量，不整体称为斜率' }, { symbol: 'x_j', meaning: '第 j 个特征值' }, { symbol: 'w_j', meaning: '第 j 个特征的权重' }, { symbol: 'b', meaning: '偏置' }, { symbol: '\\hat y', meaning: '实数预测值' }] }, { label: '训练集批量预测', formula: '\\hat{\\mathbf y}=X\\mathbf w+b\\mathbf 1_m,\\qquad X\\in\\mathbb R^{m\\times n}', explanation: '矩阵 X 的每一行是一个样本；输出 \\hat{\\mathbf y}\\in\\mathbb R^m。', symbols: [{ symbol: 'm', meaning: '训练样本数量' }, { symbol: 'n', meaning: '特征数量' }, { symbol: '\\mathbf 1_m', meaning: '长度为 m 的全 1 向量' }] }, { label: '每个权重的梯度', formula: '\\frac{\\partial J}{\\partial w_j}=\\frac1m\\sum_{i=1}^m(f_{\\mathbf w,b}(\\mathbf x^{(i)})-y^{(i)})x_j^{(i)}', explanation: '每个特征拥有对应梯度分量，全部权重与偏置应基于同一组旧参数同步更新。', symbols: [{ symbol: 'j', meaning: '特征编号' }, { symbol: 'i', meaning: '样本编号' }, { symbol: 'x_j^{(i)}', meaning: '第 i 个样本的第 j 个特征' }] }],
    algorithm: [{ title: '组织单个样本', detail: '把面积、卧室数、楼龄按固定顺序组成 \\mathbf x^{(i)}。' }, { title: '准备权重向量', detail: '\\mathbf w 与特征向量使用同一顺序和相同长度 n。' }, { title: '计算单样本点积', detail: 'np.dot(w,x) 对应 \\mathbf w^{\\mathsf T}\\mathbf x，输出标量预测值。' }, { title: '计算整批预测', detail: 'X @ w 对 m 个样本进行批量计算，输出长度为 m 的预测向量。' }, { title: '同步更新参数', detail: '对训练集汇总误差，为每个 w_j 计算梯度并与 b 同步更新。' }],
    example: { title: '三特征房价预测', setup: 'x=[120,3,10] 分别表示面积、卧室数、楼龄；w=[1.2,8,-0.5]，b=20。', steps: [{ text: '逐项贡献。', formula: '1.2\\times120=144,\\quad8\\times3=24,\\quad(-0.5)\\times10=-5' }, { text: '求和并加偏置。', formula: '\\hat y=144+24-5+20=183' }], result: '预测价格为 183 万元；楼龄权重为负表示在其他特征固定时，楼龄增加会降低预测。' },
    python: { code: `import numpy as np\n\nx = np.array([120., 3., 10.])\nw = np.array([1.2, 8.0, -0.5])\nb = 20.0\n\nloop_prediction = 0.0\nfor j in range(len(x)):\n    loop_prediction += w[j] * x[j]\nloop_prediction += b\n\nsingle_prediction = np.dot(w, x) + b\nX = np.array([[120., 3., 10.], [80., 2., 20.]])\nbatch_predictions = X @ w + b\n\nprint(x.shape, X.shape)\nprint(loop_prediction, single_prediction)\nprint(batch_predictions)`, output: '(3,) (2, 3)\n183.0 183.0\n[183. 122.]', notes: ['代码中的 x 对应单个特征向量，X 对应 m×n 特征矩阵，w 对应权重向量。', 'np.dot(w, x) 输出单个样本的标量预测，X @ w 输出长度为 m 的批量预测向量。', '循环、点积和矩阵乘法使用同一个多元线性回归模型。'] },
    interaction: 'vectorization',
    misconceptions: [{ wrong: '向量化只是把循环换个写法，没有数学意义。', why: '点积和矩阵乘法明确表达单样本与批量预测。', correct: '向量化统一数学表达，并可利用底层数值库进行批量计算。' }, { wrong: '整个训练集和单个样本形状相同。', why: '训练集包含 m 个样本，每个样本有 n 个特征。', correct: '\\mathbf x^{(i)} 的形状为 (n,)，X 的形状为 (m,n)。' }],
    recap: ['多元线性回归输出实数预测值。', 'm 表示训练样本数量，n 表示特征数量。', 'np.dot(w,x) 输出单样本标量，X @ w 输出长度为 m 的向量。', '向量化统一对应特征贡献的求和与批量计算。'],
  },
  {
    id: 'feature-scaling', number: '08', title: '特征缩放', englishTitle: 'Feature Scaling', videoRange: '视频 20—21',
    summary: '特征缩放通常能改善基于梯度方法的优化效率，但不保证预测精度提高，也不是所有训练方法都同样依赖缩放。',
    problem: '房屋面积约为 300—3000，卧室数约为 1—5。特征尺度差异常使平方误差代价函数等高线在某些方向过度狭长，使同一学习率下的梯度更新更难兼顾不同权重。',
    intuition: ['特征缩放改变数值表示和优化条件，不增加原始信息，也不保证模型的泛化能力提高。', '按选定尺度缩放把特征除以正尺度 s_j，例如训练集最大绝对值或有明确依据的代表性尺度；当 s_j 取训练集最大绝对值时，也可称为最大绝对值缩放。', '均值归一化先减均值，再除以极差。Z-score 标准化先减均值，再除以标准差；它们与按选定尺度缩放是三种不同方法。', '当 σ_j>0 且使用相同的总体标准差口径时，Z-score 标准化后训练集该列均值为 0、标准差为 1；NumPy 的 X.std(axis=0) 默认使用 ddof=0，正是本节口径。', '这里使用的正比例缩放或正标准差标准化会保持同一特征内样本的大小顺序，并按统一比例改变该特征上的距离。'],
    formulas: [
      { label: '按选定尺度缩放（Scaling by a Chosen Scale）', formula: 'x_j^{\\prime}=\\frac{x_j}{s_j},\\qquad s_j>0', explanation: '对第 j 个特征选择正尺度 s_j 后相除。s_j 可以取训练集该特征的最大绝对值，也可以取有明确依据的代表性尺度；3000 只是当前房屋面积示例所采用的尺度。', symbols: [{ symbol: 'x_j', meaning: '第 j 个原始特征' }, { symbol: 's_j', meaning: '为第 j 个特征选定的正尺度' }] },
      { label: '均值归一化', formula: 'x_j^{\\prime}=\\frac{x_j-\\mu_j}{x_{j,\\max}-x_{j,\\min}}', explanation: '先居中，再用训练集该特征的极差缩放。', symbols: [{ symbol: 'x_j', meaning: '第 j 个特征的原始值' }, { symbol: '\\mu_j', meaning: '训练集第 j 个特征的均值' }, { symbol: 'x_{j,\\max},x_{j,\\min}', meaning: '训练集该特征的最大值与最小值' }] },
      { label: 'Z-score 标准化', formula: 'x_j^{\\prime}=\\frac{x_j-\\mu_j}{\\sigma_j}', explanation: '当 σ_j>0，并使用相同的总体标准差口径计算时，变换后训练集该列均值为 0、标准差为 1。', symbols: [{ symbol: 'x_j', meaning: '原始特征值' }, { symbol: '\\mu_j', meaning: '训练集该特征均值' }, { symbol: '\\sigma_j', meaning: '训练集该特征的总体标准差，且 σ_j>0' }] },
    ],
    algorithm: [{ title: '按列计算尺度', detail: '每个特征分别计算选定尺度、均值、极差或总体标准差。' }, { title: '逐列变换', detail: '同一特征的全部训练样本使用同一组缩放参数。' }, { title: '用缩放数据训练', detail: '在变换后的特征上计算预测、代价和梯度。' }, { title: '比较优化路径', detail: '缩放通常能改善条件状况，但特征相关性仍可能使等高线保持狭长。' }],
    example: { title: '手算一个样本的三种缩放', setup: '面积训练数据为 [300,1650,3000]，待缩放值为 1650；选定尺度 s=3000，均值为 1650，极差为 2700，总体标准差约为 1102.27。', steps: [{ text: '按选定尺度缩放。', formula: '1650/3000=0.55' }, { text: '均值归一化。', formula: '(1650-1650)/(3000-300)=0' }, { text: 'Z-score 标准化。', formula: '(1650-1650)/1102.27=0' }], result: '按选定尺度缩放得到 0.55；两种居中方法得到 0。这里 s=3000 也恰好是训练集面积的最大绝对值，因此这一例还可称为最大绝对值缩放。' },
    python: { code: `import numpy as np\n\nX = np.array([[300., 1.], [1650., 3.], [3000., 5.]])\nchosen_scale = np.array([3000., 5.])\nscaled_by_choice = X / chosen_scale\n\nmean = X.mean(axis=0)\nstd = X.std(axis=0)\nz_score = (X - mean) / std\nprint(scaled_by_choice.round(3))\nprint(mean)\nprint(std.round(3))\nprint(z_score.round(3))`, output: '[[0.1  0.2 ]\n [0.55 0.6 ]\n [1.   1.  ]]\n[1650.    3.]\n[1102.27     1.633]\n[[-1.225 -1.225]\n [ 0.     0.   ]\n [ 1.225  1.225]]', notes: ['chosen_scale 分别给面积和卧室数选择尺度 3000 与 5；它们恰好是训练集各列的最大绝对值。', 'axis=0 表示分别对每个特征列计算；X.std(axis=0) 默认使用 ddof=0 的总体标准差口径，与正文公式一致。', 'Z-score 标准化保持同一列样本的大小顺序，并按同一正比例改变该列距离。'] },
    interaction: 'feature-scaling',
    misconceptions: [{ wrong: '特征缩放一定提高预测精度。', why: '它主要改变数值表示与优化条件，不增加训练信息。', correct: '缩放通常改善基于梯度方法的优化效率，但不保证泛化表现提高。' }, { wrong: '按选定尺度缩放时，所有特征都除以同一个数。', why: '不同特征可以有不同的代表性尺度。', correct: '为每一列分别选择有依据的正尺度 s_j；取该列最大绝对值只是其中一种选择。' }, { wrong: '标准差为 0 的常数特征也能直接做 Z-score 标准化。', why: '分母为 0，公式没有定义。', correct: '常数特征应单独处理，不能直接相除。' }, { wrong: '按选定尺度缩放、均值归一化和 Z-score 标准化只是同一种方法的不同名称。', why: '三者的居中方式与分母定义不同。', correct: '分别依据选定尺度、极差和标准差应用相应公式。' }],
    recap: ['特征尺度差异常使代价函数等高线过度狭长，相关性也可能造成类似形状。', '缩放通常改善基于梯度方法的优化效率，但不保证精度提高。', '按选定尺度缩放、均值归一化与 Z-score 标准化是不同方法。', '当 s_j 取训练集最大绝对值时，按选定尺度缩放也可称为最大绝对值缩放。', 'X.std(axis=0) 使用 ddof=0 的总体标准差口径。'],
    probabilityLink: { label: '概率论关联：均值、方差与标准差', to: '/knowledge/variance' },
  },
  ...regressionPracticeLessons,
  ...classificationLessons,
]

export const machineLearningScope = {
  current: '正则化逻辑回归',
  videos: '视频 1—36',
  note: '学习路线参考吴恩达 2022 版 Machine Learning Specialization；本站内容为个人复习笔记与原创交互讲解。',
}

export function getMachineLearningLesson(id: string | undefined) {
  return machineLearningLessons.find((lesson) => lesson.id === id)
}
